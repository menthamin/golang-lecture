package main

import (
	"fmt"
	"time"
)

func main() {
	// sales := make([]int, 11)
	// limit := int64(10)
	// fmt.Println(len(sales))

	// hasMore := true
	// fmt.Println(hasMore)
	// hasMore = int64(len(sales)) == limit
	// fmt.Println(hasMore)

	checkNow := time.Now()
	fmt.Println("type of checkNow is %T\n", checkNow)

}
